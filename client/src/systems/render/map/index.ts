// STUB — implement in Wave 2 per gta6-build/01-render/map-minimap.md
// Subsystem: render/map (client). Own all files under this folder.
import type { SubsystemModule } from "@sunbreak/shared";

export const map: SubsystemModule = {
  id: "render/map",
};
