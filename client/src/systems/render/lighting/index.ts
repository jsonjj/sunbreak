// STUB — implement in Wave 2 per gta6-build/01-render/lighting-sky.md
// Subsystem: render/lighting (client). Own all files under this folder.
// Register systems/modules via `registerModule` from client/src/game/registry.ts.
import type { SubsystemModule } from "@sunbreak/shared";

export const lighting: SubsystemModule = {
  id: "render/lighting",
};
