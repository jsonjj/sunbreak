// STUB — implement in Wave 2 per gta6-build/04-ai/npc-personas.md
// Subsystem: ai/npc-personas (client) — per-archetype persona prompts + client hooks.
import type { SubsystemModule } from "@sunbreak/shared";

export const npcPersonas: SubsystemModule = {
  id: "ai/npc-personas",
};
