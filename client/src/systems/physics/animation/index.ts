// STUB — implement in Wave 2 per gta6-build/02-physics/animation-system.md
// Subsystem: physics/animation (client) — Mixamo clips + blend trees via drei useAnimations.
import type { SubsystemModule } from "@sunbreak/shared";

export const animation: SubsystemModule = {
  id: "physics/animation",
};
