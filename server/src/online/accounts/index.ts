// STUB — implement in Wave 2 per gta6-build/06-online/accounts-sessions.md
// Subsystem: online/accounts (server) — lightweight accounts + sessions (v4).
import type { SubsystemModule } from "@sunbreak/shared";

export const accounts: SubsystemModule = {
  id: "online/accounts",
};
